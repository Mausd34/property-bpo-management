import React,{useState}from'react';import{createRoot}from'react-dom/client';const API=import.meta.env.VITE_API_URL||'http://127.0.0.1:8000';function App(){const[t,setT]=useState(localStorage.token||'');const[f,setF]=useState({username:'',password:''});const[tab,setTab]=useState('login');const[d,setD]=useState(null);async function auth(){let r=await fetch(API+'/auth/'+tab,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)}),x=await r.json();if(r.ok&&tab==='login'){localStorage.token=x.access_token;setT(x.access_token)}else alert(x.detail||'Done')}async function load(){let r=await fetch(API+'/dashboard');setD(await r.json())}if(!t)return <main><h1>Property & BPO Management</h1><button onClick={()=>setTab('login')}>Login</button><button onClick={()=>setTab('register')}>Register</button><input placeholder="Username" onChange={e=>setF({...f,username:e.target.value})}/><input type="password" placeholder="Password" onChange={e=>setF({...f,password:e.target.value})}/><button onClick={auth}>{tab}</button></main>;return <main><h1>Property & BPO Dashboard</h1><button onClick={load}>Load KPI</button>{d&&<pre>{JSON.stringify(d,null,2)}</pre>}<button onClick={()=>{localStorage.clear();setT('')}}>Logout</button></main>}createRoot(document.getElementById('root')).render(<App/>);