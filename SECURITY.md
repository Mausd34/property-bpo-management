# Security

Do not commit customer addresses, owner information, credentials, or production secrets.

This is a portfolio/demo workflow. Production deployment should add authentication, role-based access control, persistent transactional storage, audit logs, encryption, rate limiting, secure secret management, HTTPS, and strict tenant/data isolation.
